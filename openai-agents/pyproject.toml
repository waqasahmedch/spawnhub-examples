[project]
name = "spawnhub-example-openai-agents"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "spawnhub[openai]>=0.1.0",
    "python-dotenv>=1.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = []
